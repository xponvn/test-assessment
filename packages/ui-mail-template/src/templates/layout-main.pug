include component-button.pug

doctype html
html(
  style={
    width: '100%',
    height: '100%',
    'background-color': '#E5E5E5',
  }
)

  include layout-head.pug

  body(
    style={
      width: '100%',
      height: '100%',
      margin: 0,
    }
  )
    table(width='612px' border='0' cellspacing='0' style={
      margin: '97px auto auto auto',
    })
      tbody(style={width: '612px', 'background-color': '#F5F5F5'})
        include layout-header.pug

        block content

        include layout-footer.pug

        include layout-more-information.pug

    //- include layout-foot.pug
