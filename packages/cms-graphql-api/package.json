{
  "name": "@test-assessment/cms-graphql-api",
  "version": "0.0.0",
  "scripts": {
    "gen": "DOTENV_CONFIG_PATH=.env graphql-codegen --require dotenv/config --config codegen.ts",
    "watch": "yarn gen --watch"
  }
}
