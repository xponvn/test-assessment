import { uiExampleComponent } from './ui-example-component';

describe('uiExampleComponent', () => {
  it('should work', () => {
    expect(uiExampleComponent()).toEqual('ui-example-component');
  });
});
